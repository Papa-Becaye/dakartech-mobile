<?xml version="1.0" encoding="utf-8"?>
<!--
  Autorise explicitement le HTTP en clair (trafic non-TLS) uniquement vers les
  hôtes de développement local. Sans cette config, Android 9+ refuse tout
  `http://`, y compris vers l'émulateur (10.0.2.2 = hôte local, port 9000).

  En production, l'application doit utiliser `https://` via
  `API_BASE_URL` (dart-define) : le reste du trafic cleartext reste bloqué
  par `base-config cleartextTrafficPermitted="false"`.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>